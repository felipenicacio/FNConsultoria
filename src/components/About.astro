<section class="section about" aria-labelledby="about-title">
  <div class="container about-grid">
    <div class="about-heading">
      <p class="eyebrow">FN Security</p>
      <h2 id="about-title">Segurança como disciplina de gestão.</h2>
      <span class="rule" aria-hidden="true"></span>
    </div>
    <div class="about-copy">
      <p class="lead">A proteção das informações exige governança, gestão de riscos, processos, pessoas e decisões alinhadas aos objetivos do negócio.</p>
      <p>A FN Security apoia organizações na estruturação e evolução de programas de Segurança da Informação, transformando requisitos técnicos, riscos e referências reconhecidas em modelos de gestão aplicáveis à realidade de cada organização.</p>
      <div class="pill-row" aria-label="Princípios de atuação">
        <span>Governança</span><span>Riscos</span><span>Estratégia</span><span>Resiliência</span><span>Pessoas</span>
      </div>
    </div>
  </div>
</section>

<style>
  .about { position: relative; }
  .about-grid { display: grid; grid-template-columns: .82fr 1.18fr; gap: clamp(3rem, 7vw, 7rem); align-items: start; }
  .about-heading { position: sticky; top: 110px; }
  .rule { display: block; width: 72px; height: 2px; margin-top: 2rem; background: linear-gradient(90deg, var(--blue-600), var(--teal-500)); }
  .about-copy { padding-top: .25rem; }
  .about-copy .lead { color: var(--navy-900); }
  .about-copy > p:not(.lead) { max-width: 64ch; }
  .pill-row { display: flex; flex-wrap: wrap; gap: .55rem; margin-top: 2rem; padding-top: 1.6rem; border-top: 1px solid var(--slate-200); }
  .pill-row span { padding: .5rem .75rem; border: 1px solid var(--slate-200); border-radius: 999px; color: var(--slate-700); background: var(--white); font-size: .82rem; font-weight: 750; }
  @media (max-width: 760px) { .about-grid { grid-template-columns: 1fr; gap: 1.5rem; } .about-heading { position: static; } }
</style>
