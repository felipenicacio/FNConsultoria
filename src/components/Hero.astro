<section id="inicio" class="hero" aria-labelledby="hero-title">
  <div class="container hero-grid">
    <div class="hero-content">
      <p class="eyebrow">Consultoria especializada</p>
      <h1 id="hero-title">Segurança da Informação orientada por riscos e prioridades do negócio.</h1>
      <p class="hero-copy">A FN Security apoia organizações a estruturar e evoluir sua Segurança da Informação conectando riscos, prioridades, capacidades e decisões às necessidades reais do negócio.</p>
      <div class="actions">
        <a class="button hero-primary" href="#contato">Fale conosco</a>
        <a class="button hero-secondary" href="#servicos">Conheça os serviços <span aria-hidden="true">→</span></a>
      </div>
    </div>
    <div class="hero-visual" aria-hidden="true">
      <img src="/images/security-shield.svg" alt="" width="620" height="620" loading="eager" decoding="async" />
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    color: var(--navy-950);
    padding: clamp(5.5rem, 9vw, 8rem) 0 clamp(5rem, 8vw, 7rem);
    overflow: hidden;
    background:
      radial-gradient(circle at 74% 48%, rgba(47,123,234,.10), transparent 31%),
      linear-gradient(135deg, #fff 0%, #fbfdff 58%, #f1f7ff 100%);
  }
  .hero::before {
    content: "";
    position: absolute;
    inset: auto -8% -34% 42%;
    height: 76%;
    opacity: .45;
    background-image:
      linear-gradient(rgba(23,105,224,.08) 1px, transparent 1px),
      linear-gradient(90deg, rgba(23,105,224,.08) 1px, transparent 1px);
    background-size: 54px 54px;
    transform: perspective(560px) rotateX(58deg) rotateZ(-8deg);
    mask-image: linear-gradient(to top, #000, transparent 82%);
    pointer-events: none;
  }
  .hero-grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(360px, .95fr);
    gap: clamp(2.5rem, 6vw, 6rem);
    align-items: center;
  }
  .hero-content { max-width: 690px; }
  .hero .eyebrow { color: var(--blue-600); }
  .hero h1 {
    color: var(--navy-950);
    max-width: 14ch;
    margin-bottom: 1.5rem;
    font-size: clamp(3rem, 5.3vw, 5.15rem);
  }
  .hero-copy {
    color: var(--slate-700);
    font-size: clamp(1.08rem, 1.55vw, 1.28rem);
    max-width: 59ch;
  }
  .actions { display: flex; flex-wrap: wrap; gap: .85rem; margin-top: 2.25rem; align-items: center; }
  .hero-primary { background: var(--navy-950); color: var(--white); border: 1px solid var(--navy-950); }
  .hero-primary:hover { background: var(--blue-600); border-color: var(--blue-600); }
  .hero-secondary { color: var(--navy-950); border: 1px solid transparent; background: transparent; gap: .55rem; }
  .hero-secondary:hover { color: var(--blue-600); background: rgba(23,105,224,.06); }
  .hero-visual { position: relative; display: grid; place-items: center; min-height: 440px; }
  .hero-visual::after {
    content: "";
    position: absolute;
    width: 68%;
    height: 8%;
    bottom: 8%;
    border-radius: 50%;
    background: rgba(23,105,224,.16);
    filter: blur(22px);
    z-index: -1;
  }
  .hero-visual img { width: min(100%, 540px); height: auto; filter: drop-shadow(0 28px 45px rgba(7,17,31,.13)); }

  @media (max-width: 900px) {
    .hero { padding: 5.5rem 0 4.5rem; }
    .hero-grid { grid-template-columns: 1fr; gap: 2.25rem; }
    .hero-content { max-width: 760px; }
    .hero h1 { max-width: 15ch; }
    .hero-visual { min-height: 0; }
    .hero-visual img { width: min(76vw, 430px); }
  }
  @media (max-width: 560px) {
    .hero { padding-top: 4.5rem; }
    .hero h1 { font-size: clamp(2.55rem, 12vw, 3.65rem); }
    .actions { align-items: stretch; }
    .actions .button { width: 100%; }
    .hero-visual img { width: min(88vw, 380px); }
  }
</style>
