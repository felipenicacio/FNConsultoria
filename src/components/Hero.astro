<section id="inicio" class="hero" aria-labelledby="hero-title">
  <div class="container hero-grid">
    <div class="hero-content">
      <h1 id="hero-title">Segurança da Informação orientada por riscos e prioridades do negócio.</h1>
      <p class="hero-copy">A FN Security apoia organizações a estruturar e evoluir sua Segurança da Informação conectando riscos, prioridades, capacidades e decisões às necessidades reais do negócio.</p>
      <div class="actions">
        <a class="button button-primary" href="#servicos">Conheça os serviços</a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero { position: relative; background: var(--navy-950); color: white; padding: 8rem 0 7rem; overflow: hidden; }
  .hero::before { content: ""; position: absolute; inset: 0; background: radial-gradient(circle at 72% 28%, rgba(23,105,224,.16), transparent 28%), radial-gradient(circle at 86% 74%, rgba(26,167,161,.10), transparent 24%); pointer-events: none; }
  .hero-grid { position: relative; }
  .hero-content { max-width: 72ch; }
  .hero .eyebrow { color: #7dd3fc; }
  .hero h1 { color: white; margin-bottom: 1.5rem; }
  .hero-copy { color: #cbd5e1; font-size: clamp(1.1rem, 1.8vw, 1.32rem); max-width: 58ch; }
  .actions { display: flex; flex-wrap: wrap; gap: .8rem; margin-top: 2.3rem; }
  @media (max-width: 900px) { .hero { padding: 6rem 0 5.5rem; } }
</style>
