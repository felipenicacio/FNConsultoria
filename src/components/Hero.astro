<section id="inicio" class="hero">
  <div class="container hero-grid">
    <div>
      <p class="eyebrow">FN Consulting</p>
      <h1>Segurança da Informação conectada à estratégia do negócio.</h1>
      <p class="hero-copy">Consultoria especializada em governança, gestão de riscos, Segurança da Informação e resiliência organizacional.</p>
      <div class="actions">
        <a class="button button-primary" href="#servicos">Conheça os serviços</a>
        <a class="button button-secondary" href="#contato">Fale com a FN Consulting</a>
      </div>
    </div>
    <div class="visual" aria-hidden="true">
      <div class="orb orb-a"></div><div class="orb orb-b"></div><div class="mesh"></div>
      <div class="signal">Governança <span>→</span> Riscos <span>→</span> Estratégia</div>
    </div>
  </div>
</section>

<style>
  .hero { background: var(--navy-950); color: white; padding: 7rem 0 6rem; overflow: hidden; }
  .hero-grid { display: grid; grid-template-columns: 1.2fr .8fr; gap: 4rem; align-items: center; }
  .hero .eyebrow { color: #7dd3fc; }
  .hero h1 { color: white; }
  .hero-copy { color: #cbd5e1; font-size: clamp(1.1rem, 2vw, 1.35rem); max-width: 58ch; }
  .actions { display: flex; flex-wrap: wrap; gap: .8rem; margin-top: 2rem; }
  .visual { position: relative; min-height: 360px; border: 1px solid rgba(255,255,255,.12); border-radius: 32px; background: linear-gradient(145deg, rgba(23,105,224,.18), rgba(26,167,161,.08)); overflow: hidden; box-shadow: inset 0 0 60px rgba(255,255,255,.03); }
  .mesh { position: absolute; inset: 0; background-image: linear-gradient(rgba(255,255,255,.06) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.06) 1px, transparent 1px); background-size: 42px 42px; mask-image: linear-gradient(to bottom, black, transparent); }
  .orb { position: absolute; border-radius: 999px; filter: blur(3px); }
  .orb-a { width: 170px; height: 170px; top: 55px; left: 70px; background: rgba(23,105,224,.55); }
  .orb-b { width: 130px; height: 130px; right: 55px; bottom: 65px; background: rgba(26,167,161,.5); }
  .signal { position: absolute; left: 1.4rem; right: 1.4rem; bottom: 1.4rem; padding: 1rem 1.2rem; border-radius: 16px; background: rgba(7,17,31,.72); border: 1px solid rgba(255,255,255,.12); font-weight: 700; backdrop-filter: blur(12px); }
  .signal span { color: #5eead4; margin: 0 .45rem; }
  @media (max-width: 850px) { .hero { padding-top: 5rem; } .hero-grid { grid-template-columns: 1fr; } .visual { min-height: 260px; } }
</style>
