<!--
  Autoridade consultiva — FEL-68 / FEL-44
  Foto profissional de Felipe Nicácio substitui painel FN (aprovado no Notion 04, seção 10).
  Cargo atualizado: Fundador e Head de Consultoria
  LinkedIn mantido como elemento de autoridade profissional.
  Ativos: public/images/felipe-nicacio.webp (principal), .jpg (fallback/original)
-->
<section id="sobre" class="section profile-section" aria-labelledby="profile-title">
  <div class="container profile-grid">

    <div class="portrait-wrap">
      <picture>
        <source srcset="/images/felipe-nicacio.webp" type="image/webp" width="800" height="800" />
        <img
          src="/images/felipe-nicacio.jpg"
          alt="Felipe Nicácio, Fundador e Head de Consultoria da FN Security"
          width="800"
          height="800"
          loading="lazy"
          decoding="async"
          class="portrait-photo"
        />
      </picture>
    </div>

    <div class="profile-copy">
      <p class="eyebrow">Sobre</p>
      <h2 id="profile-title">Felipe Nicácio</h2>
      <p class="role">Fundador e Head de Consultoria da FN Security</p>
      <p class="lead">Especialista em Segurança da Informação, Continuidade de Negócios e Privacidade de Dados.</p>
      <p>Atuação em governança, gestão de riscos, implementação de sistemas de gestão, assessments de maturidade e estruturação de programas de Segurança da Informação, utilizando referências como ISO/IEC 27001, ISO/IEC 27005, ISO/IEC 27701, ISO 22301, NIST CSF e NIST RMF.</p>
      <a
        class="profile-link"
        href="https://www.linkedin.com/in/felipe-nicacio/"
        target="_blank"
        rel="noopener noreferrer"
      >
        Conecte-se no LinkedIn
        <span aria-hidden="true">→</span>
        <span class="sr-only"> (abre em nova aba)</span>
      </a>
    </div>

  </div>
</section>

<style>
  .profile-section { background: var(--white); }

  .profile-grid {
    display: grid;
    grid-template-columns: .72fr 1.28fr;
    gap: clamp(3rem, 7vw, 7rem);
    align-items: center;
  }

  /* Foto */
  .portrait-wrap {
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-soft);
    /* aspect-ratio 1:1 — reserva espaço, evita CLS */
    aspect-ratio: 1 / 1;
    background: var(--slate-100);
  }

  .portrait-photo {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
  }

  /* Texto */
  .role {
    margin: -.2rem 0 1.4rem;
    color: var(--blue-600);
    font-size: .82rem;
    font-weight: 800;
    letter-spacing: .04em;
    text-transform: uppercase;
  }

  .profile-copy > p:not(.eyebrow):not(.role) { max-width: 64ch; }

  .profile-link {
    display: inline-flex;
    align-items: center;
    gap: .65rem;
    margin-top: .8rem;
    padding-block: .4rem;
    color: var(--blue-600);
    font-weight: 800;
  }
  .profile-link span[aria-hidden] {
    color: var(--teal-500);
    transition: transform .2s ease;
  }
  .profile-link:hover span[aria-hidden] { transform: translateX(3px); }

  @media (max-width: 760px) {
    .profile-grid { grid-template-columns: 1fr; }
    .portrait-wrap { max-width: 320px; }
  }
</style>
