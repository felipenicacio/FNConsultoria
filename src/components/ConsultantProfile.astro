<section id="sobre" class="section profile-section">
  <div class="container profile-grid">
    <div class="portrait" aria-hidden="true">
      <span class="portrait-label">Consultor responsável</span>
      <strong>FN</strong>
      <span class="portrait-line"></span>
      <small>Governança · Riscos · Segurança</small>
    </div>
    <div class="profile-copy">
      <p class="eyebrow">Sobre</p>
      <h2>Felipe Nicácio</h2>
      <p class="role">Consultor responsável pela FN Security</p>
      <p class="lead">Especialista em Segurança da Informação, Continuidade de Negócios e Privacidade de Dados.</p>
      <p>Atuação em governança, gestão de riscos, implementação de sistemas de gestão, assessments de maturidade e estruturação de programas de Segurança da Informação, utilizando referências como ISO/IEC 27001, ISO/IEC 27005, ISO/IEC 27701, ISO 22301, NIST CSF e NIST RMF.</p>
      <a class="profile-link" href="https://www.linkedin.com/in/felipe-nicacio/" target="_blank" rel="noopener noreferrer">Conecte-se no LinkedIn <span aria-hidden="true">→</span><span class="sr-only"> (abre em nova aba)</span></a>
    </div>
  </div>
</section>

<style>
  .profile-section { background: var(--white); }
  .profile-grid { display: grid; grid-template-columns: .72fr 1.28fr; gap: clamp(3rem, 7vw, 7rem); align-items: center; }
  .portrait { position: relative; aspect-ratio: .9; min-height: 390px; padding: 2rem; border-radius: var(--radius-xl); display: flex; flex-direction: column; justify-content: flex-end; background: linear-gradient(145deg, var(--navy-950), var(--navy-800)); box-shadow: var(--shadow-soft); overflow: hidden; }
  .portrait::before { content: ""; position: absolute; width: 250px; height: 250px; top: 48px; right: -65px; border: 1px solid rgba(125,211,252,.18); border-radius: 50%; box-shadow: 0 0 0 50px rgba(23,105,224,.035), 0 0 0 100px rgba(26,167,161,.02); }
  .portrait-label { position: absolute; top: 1.7rem; left: 1.7rem; color: #94a3b8; font-size: .68rem; font-weight: 850; letter-spacing: .13em; text-transform: uppercase; }
  .portrait strong { position: relative; color: white; font-size: clamp(4rem, 8vw, 6.8rem); font-weight: 900; line-height: .9; letter-spacing: -.09em; }
  .portrait-line { position: relative; width: 64px; height: 2px; margin: 1.5rem 0 1rem; background: linear-gradient(90deg, var(--blue-500), #5eead4); }
  .portrait small { position: relative; color: #cbd5e1; font-size: .78rem; font-weight: 650; }
  .role { margin: -.2rem 0 1.4rem; color: var(--blue-600); font-size: .82rem; font-weight: 800; letter-spacing: .04em; text-transform: uppercase; }
  .profile-copy > p:not(.eyebrow):not(.role) { max-width: 64ch; }
  .profile-link { display: inline-flex; align-items: center; gap: .65rem; margin-top: .8rem; padding-block: .4rem; color: var(--blue-600); font-weight: 800; }
  .profile-link span { color: var(--teal-500); transition: transform .2s ease; }
  .profile-link:hover span { transform: translateX(3px); }
  @media (max-width: 760px) { .profile-grid { grid-template-columns: 1fr; } .portrait { max-width: 380px; min-height: 330px; } }
</style>
